---
id: SR-003
title: "Spec frontmatter as spec nodes"
statement: "The system shall treat a spec carrying id/title/status frontmatter as a canonical spec:<id> trace node, failing deterministically on duplicate ids with differing content, so requirements can express coverage over specs."
domain: behavioral
upstream: []
source: "docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#10"
acceptance:
  - id: AC-1
    criterion: "A spec carrying id, title and status frontmatter is loaded as a canonical spec node addressed by its declared id."
    verification:
      kind: test_marker
      ref: "tests/unit/coherence/test_artifact_families.py"
  - id: AC-2
    criterion: "A plan's reference to a spec resolves to that spec's canonical frontmatter id and never to an id derived from the spec's file name, whether the reference is a frontmatter field or a literal path in the body."
    verification:
      kind: test_marker
      ref: "tests/unit/coherence/test_artifact_families.py"
  - id: AC-3
    criterion: "Two specs declaring the same id with differing content raise a deterministic load error instead of one silently winning."
    verification:
      kind: test_marker
      ref: "tests/unit/coherence/test_artifact_families.py"
---

Source: docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#10.
