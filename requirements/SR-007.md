---
acceptance:
- criterion: A knowledge-base entry scoped only by error signature is selected and
    reaches the agent prompt when a real gate failure produces a matching signature,
    and is not selected when the failure signature does not match or no gate failed.
  id: AC-1
  verification:
    kind: test_marker
    ref: tests/unit/orchestrator/test_runner.py
- criterion: A knowledge-base entry scoped only by a qualified symbol is selected
    when the changed files reach that symbol through import edges, with no file-glob
    match.
  id: AC-2
  verification:
    kind: test_marker
    ref: tests/unit/test_kb_retrieval.py
binding:
  assert: every kind:test_marker acceptance criterion's referenced test passes
  experiment: tests/unit/orchestrator/test_runner.py
  metric: pytest.mark.sr acceptance-criteria pass rate
  trials: 1
checksum: sha256:bc1add89cd7d8c9b0b56fde64b12a551f979401a2a822eb030faff9086b9fd06
domain: behavioral
id: SR-007
source: docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#9.2
statement: The system shall let knowledge-base entries be selected by error-signature
  and by the symbols or modules a change reaches, so failure records built for retrieval
  actually fire.
title: Knowledge base signature retrieval
upstream: []
---

Source: docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#9.2.

**Bound (2026-09-03).** `binding.experiment` names only AC-1's file
(`tests/unit/orchestrator/test_runner.py`) — see [[SR-001]]'s own bound note for why: the
marker-presence gate checks one named file as a sanity anchor, not an exhaustive test inventory.
AC-2's file (`tests/unit/test_kb_retrieval.py`) is covered by the requirement's measured evidence
(`evidence/runs/*.json`), not the binding.