---
id: SR-004
title: "Single code map with import edges"
statement: "The system shall provide one code map that merges symbol indexing with import edges so that requirement coverage can compute overlap from a single parser across the supported languages."
domain: behavioral
upstream: []
source: "docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#9.1"
acceptance:
  - id: AC-1
    criterion: "Requirement coverage computes import overlap from a single merged code map whose import edges are persisted beside the same fingerprinted symbol index, not from a second parser of its own."
    verification:
      kind: test_marker
      ref: "tests/unit/substrate/test_codemap_imports.py"
  - id: AC-2
    criterion: "A selection naming a missing or renamed binding test is reported as a distinct unresolved outcome, never as a resolved selection that happens to overlap nothing."
    verification:
      kind: test_marker
      ref: "tests/unit/substrate/test_codemap_imports.py"
  - id: AC-3
    criterion: "Requirement coverage computes import overlap for every language the code index parses, not for Python alone."
    verification:
      kind: manual
      reason: "No automated coverage exists and the behaviour is currently absent: the signature index parses python, javascript, typescript, tsx and rust, while the import-edge layer returns status unsupported for any non-Python root. Until the edge layer gains those languages a human must confirm, for each language the index parses, that requirement coverage either produces a real overlap answer or declares that language explicitly out of scope, and must not read an unsupported result as an absence of overlap."
---

Source: docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#9.1.
