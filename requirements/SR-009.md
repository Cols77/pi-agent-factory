---
id: SR-009
title: "Compiled obligation contract"
statement: "The system shall expose each compiled obligation with id, scope ref, kind, requiredness, reason, source policy, state, and a fully-substituted resolve command, consumed by health, navigator, and gates without reinterpreting the profile."
domain: behavioral
upstream: []
source: "docs/superpowers/specs/2026-08-22-coherence-progressive-assurance-design.md#4"
acceptance:
  - id: AC-1
    criterion: "A compiled Obligation exposes exactly the documented fields (id, scope_ref, kind, requiredness, reason, source_policy, state, resolve_cmd), with requiredness constrained to not_applicable | advisory | required | blocking."
    verification:
      kind: test_marker
      ref: "tests/unit/substrate/policy/test_obligation.py"
  - id: AC-2
    criterion: "A compiled ci_verification obligation's resolve_cmd is fully substituted -- no literal `{python}` placeholder survives -- and the gate order and duplicates of the source config are preserved verbatim."
    verification:
      kind: test_marker
      ref: "tests/unit/coherence/policy/test_compiler.py"
  - id: AC-3
    criterion: "coherence.navigate.health computes its obligation-backed health dimensions (verification strategy / executed evidence) from the compiled obligation set returned by compile_obligations for each SR scope, never by re-deriving profile logic itself."
    verification:
      kind: test_marker
      ref: "tests/unit/coherence/test_health_dimensions.py"
  - id: AC-4
    criterion: "The navigator's effective-profile/obligation view (coherence.navigate.obligations) presents the same compiled obligation set compile_obligations returns for a scope, without recomputing profile resolution independently."
    verification:
      kind: test_marker
      ref: "tests/unit/coherence/test_navigate_obligations.py"
  - id: AC-5
    criterion: "CI's required command list (coherence.policy.ci) draws its gate commands only from compiled obligations of kind ci_verification with requiredness blocking, sourced from the same compiler every other consumer uses -- never a hand-maintained gate step list -- with two fixed structural checks (`coherence trace check`, `coherence register check`) appended after them, unconditionally, not obligation-derived."
    verification:
      kind: test_marker
      ref: "tests/unit/coherence/policy/test_ci.py"
---

Source: docs/superpowers/specs/2026-08-22-coherence-progressive-assurance-design.md#4.

**Statement corrected during authoring consent (2026-09-03).** It originally listed "status,
health, inbox, navigator, and gates" as the consumers of the compiled `Obligation` contract,
quoting the source design doc's §4 verbatim. Grepping `src/coherence` for
`compile_obligations`/`Obligation` usage confirms three of the five: `coherence.navigate.health`
(dimensions 4/5 and 11), `coherence.navigate.obligations` (the navigator's effective-profile
view, wired into `coherence.navigate.cli`), and `coherence.policy.ci` (`required_ci_commands`).
The other two do not exist in the current code: `status_snapshot` (`src/coherence/status.py`) and
`coherence register status` (`src/coherence/register/cli.py`) have zero references to obligations
or the compiler, and `list_items` (`src/coherence/inbox.py`) likewise never consumes a compiled
obligation. Dropped "status" and "inbox" from the statement rather than keep an unbacked claim —
no acceptance criterion referenced either, so none needed changing beyond the statement text
itself.

Wiring `status.py`'s probes and `inbox.py`'s `list_items` to the compiled obligation set is a real
design decision (which obligations surface in a status line or inbox item, at what requiredness
threshold, for which scope) that no increment in the design doc's own integration map (§10)
assigns: Increment 2C covers only CI/gates, 3B only the navigator, 5 only the health vector. That
remains open follow-on work for a future SR, not something this authoring pass can close by
drafting an acceptance criterion no test could honestly back today.

**AC-5 narrowed during adversarial review (2026-09-03).** It originally claimed CI's required
command list is built "only" from compiled `ci_verification`/`blocking` obligations and is "never
a hand-maintained step list," full stop. `src/coherence/policy/ci.py`'s `required_ci_commands`
contradicts that as literally written: after extending `commands` with the compiled obligations'
`resolve_cmd`s, it unconditionally appends two hardcoded strings, `"coherence trace check"` and
`"coherence register check"` — a real, if small, hand-maintained tail the bound test itself
(`test_includes_every_declared_gate_command_in_order_with_python_substituted`) already works
around by slicing `commands[:-2]` before asserting the compiled portion, and that a second,
unmarked test (`test_structural_checks_are_always_appended`) confirms is unconditional. Reworded
AC-5 to claim only what the code and test prove: the *gate* commands are exclusively
obligation-derived (never hand-maintained), and the two structural checks are a separate,
unconditional, non-obligation-derived tail. No test changed — the existing bound test already
supports the narrower claim; only the criterion text was corrected.
