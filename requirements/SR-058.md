---
id: SR-058
title: "Cross-FEAT/SR semantic overlap detection at scale"
statement: "The system shall surface, as a review finding for a human to decide rather than an automatic merge, a pair of requirements whose behavioral claims plausibly overlap despite carrying no declared or narrated relation to each other, at a corpus scale where exhaustive pairwise comparison is not viable, and shall run this detection as part of the workflow that plans new features and requirements so a plausible duplicate surfaces before it is committed, not only in retroactive audits."
domain: behavioral
upstream: []
source: "docs/superpowers/specs/2026-09-03-cross-feat-semantic-overlap-detection-seed.md"
acceptance:
  - id: AC-1
    criterion: "A pair of requirements whose behavioral claims plausibly overlap, carrying no declared or narrated relation to each other, is surfaced as a review finding for a human to decide -- never applied as an automatic relation or automatic merge -- at a corpus scale where exhaustive pairwise comparison between every requirement is not viable."
    verification:
      kind: manual
      reason: "No candidate-generation or similarity mechanism exists anywhere in this codebase: nothing embeds requirement text, nothing computes nearest neighbours, and nothing narrows a corpus-scale comparison to a reviewable candidate list. A human must judge overlap by inspection until a mechanism exists. Once built (see the seed doc's recon: embeddings + nearest-neighbour candidates + a model-verified shortlist), this is test_marker-able against an engineered fixture corpus containing a deliberate near-duplicate pair with no declared relation between them, asserting it is surfaced and a genuinely unrelated pair is not."
  - id: AC-2
    criterion: "This detection runs as part of the workflow that plans new features and requirements, so a plausible duplicate surfaces while a new SR or FEAT is being drafted, not only when a retroactive audit is run separately."
    verification:
      kind: manual
      reason: "FEAT-017 PLANNING-BOOTSTRAP's authoring workflow does not invoke any overlap check today, and AC-1's mechanism does not exist to be invoked. Once both exist, this is test_marker-able by asserting the planning workflow's own pipeline calls the detector rather than requiring a human to remember to run it separately."
---

Source: [[docs/superpowers/specs/2026-09-03-cross-feat-semantic-overlap-detection-seed|Cross-FEAT/SR semantic overlap detection at scale — seed]].

Discovered during [[SR-004]]'s authoring-consent decision (2026-09-03), alongside [[SR-057]]:
having found that SR-050 reuses [[SR-023]] and extends [[SR-049]] with no typed relation recording
either link, the human asked the broader question — at hundreds or thousands of SRs and FEATs, how
do you reliably catch overlap **nobody wrote down anywhere**, where neither requirement's author
knew about the other and so no prose mention exists for a mirror check to find? That is a
different detection problem from [[SR-057]]'s (which finds gaps between what was declared and what
was narrated): this one finds overlap between what was written and other things also written, with
no declared or narrated link between them at all. Kept as a separate SR from [[SR-057]]
deliberately — [[SR-057]]'s mechanism is deterministic and `test_marker`-able; this one is
necessarily similarity-based and needs a judgement call on every candidate, never a bare pass/fail.

**Acceptance criteria added on review (2026-09-03).** Earlier registered with zero ACs; corrected
for the same reason as [[SR-057]] — not having chosen the similarity mechanism's implementation
(embeddings vs. another technique, the exact threshold) is not a reason to avoid stating the
observable contract. AC-1/AC-2 above state that contract without committing to a mechanism, both
`manual` and honestly reasoned as unimplemented. The seed doc above still records technical recon
toward the eventual design — a two-layer shape (deterministic structural completeness, which is
[[SR-057]]'s territory, plus embedding-based candidate generation narrowed by an intersection with
SR-057's declared-relation data, verified by a model only on the surviving candidates, never
auto-applied) and an explicit "not required, SQLite if anything, never a second system of record"
answer to the storage/tech-stack question — but that recon is not binding, and where the
similarity threshold sits waits for someone to plan it against real corpus scale.
