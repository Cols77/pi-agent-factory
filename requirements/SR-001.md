---
acceptance:
- criterion: Navigating from a feature anchor returns the connected system requirements,
    design decisions, implementation task and its run, metric ids, evidence and current
    per-requirement validation state, and reports no artifact that is not declared
    or recorded.
  id: AC-1
  verification:
    kind: test_marker
    ref: tests/unit/system/test_feature.py
- criterion: A requirement with no linked validation test, a requirement with no linked
    implementation, and an implementation with no traceable requirement each surface
    as a named engineering gap rather than being omitted.
  id: AC-2
  verification:
    kind: test_marker
    ref: tests/unit/system/test_vcycle_health.py
- criterion: Every id declared in a requirement's upstream field is mirrored as at
    least one [[id]] wikilink somewhere in that requirement's Markdown body.
  id: AC-3
  verification:
    kind: test_marker
    ref: tests/unit/requirements/test_register.py
binding:
  assert: every kind:test_marker acceptance criterion's referenced test passes
  experiment: tests/unit/system/test_feature.py
  metric: pytest.mark.sr acceptance-criteria pass rate
  trials: 1
checksum: sha256:c6706af200cc2f5b6ba478ba3264434b520a1f0977cb0cd9e8109da4644dae7b
domain: behavioral
id: SR-001
source: docs/superpowers/plans/engineering-context/00-high-level-requirements.md#HLR-02
statement: Where corresponding artifacts exist, the factory shall support navigation
  across system requirement, feature/design decision, production implementation symbols,
  validation test nodes, experiment/simulation run, metric, evidence, and current
  validation state through explicit declared relations maintained by implementation
  work and mirrored as Obsidian wikilinks.
title: Explicit lifecycle traceability
upstream: []
---

Source: docs/superpowers/plans/engineering-context/00-high-level-requirements.md#HLR-02.

The implementation and validation relation contract is detailed in [[docs/superpowers/specs/2026-08-31-sr-code-validation-traceability-design#canonical-relations|SR code and validation traceability design — canonical relations]].

**AC-3 was narrowed during authoring consent (2026-09-02).** It originally also claimed the
reverse direction — "no wikilink in the body names a relation the typed fields do not declare" —
as a `manual` criterion. Dropped, not kept manual: the schema has exactly one typed relation field
(`upstream`), and requirement bodies routinely and legitimately wikilink other nodes for narrative
context that is not an upstream dependency (see [[SR-050]]'s own body, linking [[SR-001]] and
[[SR-023]] in prose with `upstream: []`). Enforcing the reverse claim as written would flag that
normal, correct usage. It becomes well-defined once a broader typed-relation vocabulary exists
(`relates_to`, `distinguished_from`, …) — that vocabulary is [[SR-057]]'s territory, not this SR's.
`missing_upstream_wikilinks()` (`src/coherence/register/register.py`) implements the forward
direction that survived.

**Correction (2026-09-03):** this note originally cited SR-056 for the broader typed-relation
vocabulary. SR-056 was registered the same day but turned out to be an unrelated capability
([[SR-056]] progressive review-decision presentation); the citation above is corrected to
[[SR-057]], the requirement actually registered for this vocabulary gap.

**Bound (2026-09-03).** `binding.experiment` names only AC-1's file
(`tests/unit/system/test_feature.py`) — the design's own gate (`coherence register bind`'s
`verify_sr_marker`) checks a single named file for this SR's `@pytest.mark.sr` marker as a
sanity anchor, not an exhaustive test inventory; the requirement's actual measured pass/fail
already covers AC-2's and AC-3's files too (`tests/unit/system/test_vcycle_health.py`,
`tests/unit/requirements/test_register.py`) via `evidence/runs/*.json`'s `artifacts` list, not
the binding. No existing `binding:` anywhere in this repo established a convention for a
multi-file SR before this one; AC-1's file was picked as the requirement's primary claim, by
this repo's own convention of ordering acceptance criteria.