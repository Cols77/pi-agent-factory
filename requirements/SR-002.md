---
acceptance:
- criterion: Requirements are parsed from their authoritative Markdown files into
    a register ordered by requirement id, and a lookup for an id the register does
    not hold returns nothing rather than a fabricated entry.
  id: AC-1
  verification:
    kind: test_marker
    ref: tests/unit/requirements/test_register.py
- criterion: The register emits an index naming every requirement with its checksum,
    and a requirement carrying no binding is indexed as proposed rather than stamped
    with one.
  id: AC-2
  verification:
    kind: test_marker
    ref: tests/unit/requirements/test_cli.py
- criterion: A requirement that no measurement, linked task or deferral accounts for
    is reported as pending with a gate-failing severity rather than passing silently.
  id: AC-3
  verification:
    kind: test_marker
    ref: tests/unit/requirements/test_closure.py
binding:
  assert: every kind:test_marker acceptance criterion's referenced test passes
  experiment: tests/unit/requirements/test_register.py
  metric: pytest.mark.sr acceptance-criteria pass rate
  trials: 1
checksum: sha256:cdd91c95971c05f0d5900667f5fcdd8a92c13aa9f2c7c2ee9a44f593d42d3a63
domain: behavioral
id: SR-002
source: docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#4.2
statement: The system shall maintain a requirement register over SR nodes parsed from
  authoritative Markdown, with an index and a closure model that evaluates each requirement
  as proposed, measured, or accounted for by a task or deferral.
title: Requirement register closure
upstream: []
---

Source: docs/superpowers/specs/2026-08-18-coherence-toolset-design.md#4.2.

**Statement corrected during authoring consent (2026-09-02).** It originally claimed the register
spans "SR and BR nodes." §10 of the same source document says otherwise, verbatim: "a `BR-*` tier
is explicitly out of scope here." The code agrees with §10 — no BR handling exists anywhere in
`src/coherence/register/`. No acceptance criterion referenced BR, so none needed changing.

**Bound (2026-09-03).** `binding.experiment` names only AC-1's file
(`tests/unit/requirements/test_register.py`) — see [[SR-001]]'s own bound note for why: the
marker-presence gate checks one named file as a sanity anchor, not an exhaustive test inventory.
AC-2's and AC-3's files (`tests/unit/requirements/test_cli.py`,
`tests/unit/requirements/test_closure.py`) are covered by the requirement's measured evidence
(`evidence/runs/*.json`), not the binding.