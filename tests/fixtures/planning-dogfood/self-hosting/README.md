# Labelled repository self-hosting planning fixture

This directory is a deterministic manifest for running FEAT-017 against this repository's
canonical planning artifacts. Paths are intentionally relative to the repository root; the
harness resolves and hashes them at runtime. The expected parity/trace findings describe
existing repository health debt and are not claims that FEAT-017 is clean or approved.
