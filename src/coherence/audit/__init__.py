"""Canonical requirement coverage-audit surfaces (feature scope resolution,
per-SR classification, gate evaluation, human reporting, and the
deterministic run orchestrator), moved from ``factory.coverage``.
"""
