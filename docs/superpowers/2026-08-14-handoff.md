# HANDOFF — Resume After /clear (2026-08-14)

> **Open this first next session.** This is the single resume artifact. It records
> what is done, what genuinely remains, the working-tree state to be careful of, and
> where all the detail lives. Read the linked docs before acting; verify the current
> git state (things may have moved) rather than trusting the commit hashes verbatim.

## 1. What was just finished (committed on branch `feat/system-comprehension-layer`)

Three linked workstreams under the "context handoff" theme are **done and committed**:

- **Option A — shared pipeline node registry + transition-driven grill offer** `c32cdc0`
  - `pi-ext/factory-watch/node-registry.json` (+ `src/node-registry.ts` loader) = single
    source of truth for the pipeline node graph; dashboard/status labels + order derive
    from it → the **grill now renders** in mission control.
  - `src/pipeline-diff.ts` (`diffBlocked`) + `index.ts` transition watcher → the grill is
    offered the **moment it blocks, even if mission control is already open** (previously
    you had to re-run `/factory-watch`).
- **Item 1 — content-bearing context packet** `bc79955`
  - `src/factory/orchestrator/context_packet.py` materializes the gatherer's manifest
    (`source_files` pointers) into a token-budgeted packet: primary (Modify/Create) files
    in full, reference files as signature summaries. Persisted to
    `<transcript_dir>/context-packet.json`, threaded into Dev (`compose_prompt`), Review,
    and read by the grill seed via the extension (`readContextPacket`/`renderPacketSlice`).
  - Downstream nodes stop re-reading the codebase from zero; degrades to path bullets /
    task-text-only seed when no packet.
- **Item 2 — durable code index** `5975208`
  - `src/factory/codeindex/` — hash-keyed symbol index under `.factory/code-index/`.
    **Recomputed on every session open only when the code changed**: the fingerprint is
    the cheap checksum (hash-only); `ensure_fresh()` reuses a matching index, rebuilds on
    change. `/factory-init` seeds a full build; the extension `session_start` hook
    (`reason === "startup"`) runs `factory.codeindex --ensure`. Item 1's packet consumes
    fresh-index signatures for reference files.
- Roadmap updated in `docs/superpowers/2026-08-14-context-handoff-roadmap.md`.

Detail/spec/plan docs (all committed):
- `docs/superpowers/specs|plans/2026-08-14-pipeline-node-registry-*.md`
- `docs/superpowers/specs|plans/2026-08-14-context-packet-*.md`
- `docs/superpowers/specs|plans/2026-08-14-code-context-bundle-*.md`

## 2. Remaining work (the actual "eventual work" to continue)

The core ask is complete. What remains is **optional / opportunistic**, in priority order:

1. **Re-verify via a clean subagent run.** The subagent tool failed in this session
   (two attempts, `(no stderr)` — child spawn broken on this Windows setup), so both item
   plans were executed directly in-session. If a subagent works in a fresh environment,
   re-run each item plan through subagent-driven review for a second independent pass.
2. **Make tree-sitter actually engage.** `tree_sitter_languages` 1.10.2 wheels are
   ABI-mismatched with the top-level `tree-sitter` binding here (tried 0.24/0.25/0.26) →
   the engine honestly reports `stdlib-ast` (fully functional). Fixable options:
   pin a compatible tree-sitter version, build an ABI-matched wheel, or use a different
   tree-sitter binding. Optional; the fallback is correct.
3. **Optional upgrades from the web research** (would replace/upgrade the index
   consumption path; the index JSON format is stable for this):
   - PageRank-ranked retrieval over the index (aider-style).
   - An MCP / local query server instead of the bundled static artifact.

## 3. Pre-existing problems NOT introduced by this work (leave as-is unless tasked)

- **2 failing vitest tests, baseline-identical (verified on a clean stash):**
  - `test/smoke.test.ts` → "mission-control-review.ts loads under real `node`" (STACK_TRACE)
  - `test/system-page.test.ts` → guide-tab prose fallback (client-side synthesizes probe)
- **`pyright` baseline ~19 errors** in branch files not touched here: `system/{cli,adr}.py`,
  `trace/model.py`, `goals/{schema,registry,evaluator}.py`, `orchestrator/ledger.py`,
  `simulation/cli.py`, `factory/system/*` comprehension work. None are in the code I added.
- Note: I removed an unused import in another session's untracked `tests/unit/system/test_remediation.py`
  to keep `ruff check .` green.

## 4. Working-tree state to be careful about (concurrent in-flight work)

As of 2026-08-14 the branch has **uncommitted modifications from a parallel session**
(system-navigator comprehension layer work) in addition to session artifacts:
- Modified: `pi-ext/factory-watch/src/system-bootstrap.ts`,
  `pi-ext/factory-watch/src/system-comprehension.ts`,
  `pi-ext/factory-watch/src/system-renderers.ts`, `uv.lock` (tree-sitter install), and
  `tests/unit/system/test_remediation.py`.
- Example: `src/eng-context-tools.ts` is untracked and currently fails `tsc` (line 72) —
  not part of my commits.

**Do not `git add -A` blindly.** Stage only the specific files you intend. Prefer
reviewing the diff before any commit.

## 5. Key environment fact
- `tree_sitter_languages` wheels are ABI-mismatched with top-level `tree-sitter` on this
  machine → the code index reports `stdlib-ast` and still works. Revisit if you want
  multi-language tree-sitter parsing.

## 6. Quick resume commands
```
# gates (this repo's own .factory/factory.yaml)
uv run python -m pytest -m unit -q --ignore=tests/gates
cd pi-ext/factory-watch && npx vitest run   # 872 pass / 2 pre-existing fail
cd pi-ext/factory-watch && npx tsc --noEmit  # clean apart from untracked eng-context-tools.ts
uv run ruff check .
# code index
uv run python -m factory.codeindex --root .          # full build
uv run python -m factory.codeindex --root . --ensure # no-op when unchanged
```
Session-memory (`.pi/factory/session-memory.json`) has a matching note (`sm-0004`) pointing
here; expiry 2026-08-28.
